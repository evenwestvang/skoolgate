#!/usr/bin/env ruby

# This is Bundler-aware wrapper around thin. We use it instead of 'bundle exec'.

# Require Bundler
require 'rubygems'
require 'bundler'
Bundler.setup

# Start Thin
require 'thin'
Thin::Runner.new(ARGV).run!
